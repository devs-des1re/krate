{
  "name": "krate",
  "version": "0.0.1",
  "private": true,
  "description": "Go-native site generator with signal-based reactivity.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=20"
  },
  "homepage": "https://krate.js.org",
  "repository": {
    "type": "git",
    "url": "https://github.com/kratejs/krate.git"
  },
  "bugs": {
    "url": "https://github.com/kratejs/krate/issues"
  },
  "keywords": [
    "ssg",
    "static-site-generator",
    "go",
    "typescript",
    "tsx",
    "jsx",
    "signals",
    "compiler"
  ],
  "scripts": {
    "build": "pnpm sync:docs && pnpm -r build",
    "build:platforms": "node scripts/build-platform-packages.mjs",
    "sync:docs": "node scripts/sync-krate-docs.mjs",
    "check:docs": "node scripts/sync-krate-docs.mjs --check",
    "dev": "cd packages/compiler && go run ./cmd/krate dev ../../examples",
    "test": "pnpm sync:docs && cd packages/compiler && go test ./...",
    "test:compiler": "pnpm sync:docs && cd packages/compiler && go test ./... -v",
    "vet": "pnpm sync:docs && cd packages/compiler && go vet ./..."
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
